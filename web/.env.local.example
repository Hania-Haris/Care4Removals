# Copy this file to .env.local and fill in real values.
# .env.local is gitignored by create-next-app by default — never commit it.

# ---- Client (safe to expose — see SECURITY_MODEL.md) ----
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=care4removals-fd53c.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=care4removals-fd53c
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=care4removals-fd53c.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# ---- Server-only — NEVER prefix these NEXT_PUBLIC_, never commit real values ----

# Base64-encoded service account JSON from Firebase Console >
# Project Settings > Service Accounts > Generate new private key.
# Encode with: base64 -i service-account.json | tr -d '\n'
FIREBASE_SERVICE_ACCOUNT_KEY=

# Phase 7 — Resend (free tier), see DECISIONS_REQUIRED.md #12
RESEND_API_KEY=
LEAD_NOTIFICATION_EMAIL=m.tayyabi2822@gmail.com
